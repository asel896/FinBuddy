GEMINI_API_KEY=AIzaSyAVUfgb9dbBakcWk-NDQAR-I0RBiQHwRGI

// server.js
app.post("/api/analyze-receipt", upload.single("file"), async (req, res) => {
  const base64 = req.file.buffer.toString("base64");
  const response = await fetch(
    `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${process.env.GEMINI_API_KEY}`,
    { method: "POST", body: JSON.stringify({ /* mevcut prompt */ }) }
  );
  const data = await response.json();
  res.json(JSON.parse(data.candidates[0].content.parts[0].text));
});